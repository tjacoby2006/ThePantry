version: '3.8'

services:
  pantry:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./data/db:/db
      - ./data/uploads:/uploads
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped