@page "/settings"
@using ThePantry.Services
@using MediatR
@using ThePantry.Application.Commands
@inject ToastService ToastService
@inject NavigationManager NavigationManager
@inject IMediator Mediator

<PageTitle>Settings - The Pantry</PageTitle>

<h1>Settings</h1>

<div class="row">
    <div class="col-md-6">
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0">Data Management</h5>
            </div>
            <div class="card-body">
                <h6>Database Backup</h6>
                <p class="text-muted small">Download a copy of your SQLite database file. This contains all your inventory, history, and scan data.</p>
                <a href="/api/backup" class="btn btn-primary" target="_blank">
                    <span class="oi oi-cloud-download"></span> Download Backup
                </a>

                <hr />

                <h6>Resync Inventory Data</h6>
                <p class="text-muted small">Update existing inventory items with the latest information from the product lookup service. This will populate missing images and descriptions.</p>
                <p class="text-muted small"><i>Due to API limits, resyncing is limited to 50 items per minute.</i></p>
                <button class="btn btn-secondary" @onclick="ResyncInventory" disabled="@isResyncing">
                    @if (isResyncing)
                    {
                        <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                        <span> Resyncing...</span>
                    }
                    else
                    {
                        <span class="oi oi-reload"></span>
                        <span> Resync from Service</span>
                    }
                </button>

                <hr />

                <h6>Restore Data</h6>
                <p class="text-muted small">To restore data, replace the <code>thepantry.db</code> file in the application root with your backup file and restart the application.</p>
                <div class="alert alert-warning">
                    <span class="oi oi-warning"></span> Manual restoration is currently required for safety.
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card mb-4">
            <div class="card-header">
                <h5 class="mb-0">About ThePantry</h5>
            </div>
            <div class="card-body">
                <p><strong>Version:</strong> 1.0.0</p>
                <p>A smart pantry management system with UPC scanning and automated shopping lists.</p>
                <p>Built with Blazor Server, EF Core, and MediatR.</p>
            </div>
        </div>
    </div>
</div>

@code {
    private bool isResyncing = false;

    private async Task ResyncInventory()
    {
        isResyncing = true;
        try
        {
            var updatedCount = await Mediator.Send(new ResyncInventoryCommand());
            await ToastService.ShowSuccess($"Resync complete. Updated {updatedCount} items.");
        }
        catch (Exception ex)
        {
            await ToastService.ShowError($"Error during resync: {ex.Message}");
        }
        finally
        {
            isResyncing = false;
        }
    }
}
